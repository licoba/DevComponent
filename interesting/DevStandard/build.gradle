apply plugin: 'java-library'

dependencies {
    implementation deps.dev.dev_java

    // Gson https://github.com/google/gson
    implementation deps.lib.gson
}

// 编码设置
tasks.withType(JavaCompile) {
    options.encoding = "UTF-8"
}