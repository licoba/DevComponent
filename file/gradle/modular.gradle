ext {
    // 模块化配置信息 ( 方便维护不写在 gradle.properties )
    modularConfig = [

            // ==================
            // = project config =
            // ==================

            // 是否打印日志
            isOpenLog = true,
            // 是否 Debug 模式
            isOpenDebug = true,
            // 是否开启优化检测、调试工具 ( 控制 UETool、LeakCanary、BlockCanary、Glance 开关 )
            showDebugTools = false,
            // 模块化编译开关 ( true = 每个模块都是独立应用, false = 整合运行)
            isModular = false,
    ]

    // 每个模块各自信息
    modularVersion = [

    ]
}